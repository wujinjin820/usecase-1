# 🚀 快速部署指南

## 当前项目状态
- ✅ 项目位置：`/Users/wujinjin/Documents/GitHub/usecase-1`
- ✅ 当前分支：`usecase-2`
- ✅ 配置文件：`vercel.json` 已优化
- ✅ 所有文件已准备好

## 方法一：Vercel 网页拖拽部署（推荐）

### 步骤：

1. **打开 Vercel 部署页面**
   - 已为你打开：https://vercel.com/new
   - 如果未登录，请先登录账号

2. **拖拽文件夹**
   - 在 Finder 中找到：`/Users/wujinjin/Documents/GitHub/usecase-1`
   - 将整个文件夹拖拽到 Vercel 页面的拖拽区域
   - 或者点击 "Browse" 按钮选择文件夹

3. **配置项目**
   - Framework Preset: **Other** 或 **Vite**（如果没有 Other）
   - Build Command: **留空**
   - Output Directory: `./` 或留空
   - Root Directory: `./` 或留空
   - Install Command: **留空**

4. **部署**
   - 点击 "Deploy" 按钮
   - 等待 1-2 分钟完成部署

5. **获取链接**
   - 部署完成后会显示类似：`https://usecase-1-xxx.vercel.app`
   - 这就是你的网站链接！

## 方法二：通过 GitHub 部署（如果已连接）

1. **推送代码到 GitHub**
   ```bash
   git add .
   git commit -m "Ready for deployment"
   git push origin usecase-2
   ```

2. **在 Vercel 中导入 GitHub 仓库**
   - 访问：https://vercel.com/new
   - 选择 "Import Git Repository"
   - 选择 `usecase-1` 仓库
   - 选择分支：`usecase-2`
   - 点击 "Deploy"

## 方法三：使用 Netlify（备选）

项目中也包含 `netlify.toml` 配置文件，可以使用 Netlify 部署：

1. 访问：https://app.netlify.com/drop
2. 拖拽 `/Users/wujinjin/Documents/GitHub/usecase-1` 文件夹
3. 等待部署完成

---

**推荐使用方法一（拖拽部署）**，最简单快速！

部署完成后，你会得到一个类似 `https://your-project.vercel.app` 的链接，可以分享给任何人访问。
